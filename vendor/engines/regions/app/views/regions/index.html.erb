<% content_for :body_content_left do %>
  <ul id="regions">
    <% @regions.each do |region| %>
      <li>
        <%= link_to region.title, region_url(region) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
