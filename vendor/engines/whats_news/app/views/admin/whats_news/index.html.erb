<div id='actions'>
  <ul>
    <% if Admin::WhatsNewsController.searchable? %>
      <li>
        <%= render :partial => "/shared/admin/search",
                    :locals => {
                      :url => admin_whats_news_url
                    } %>
      </li>
    <% end %>
    <li>
      <%= link_to t('.create_new'), new_admin_whats_new_url,
                   :class => "add_icon" %>
    </li>
  <% if !searching? and WhatsNew.count > 1 and Admin::WhatsNewsController.sortable? %>
    <li>
      <%= link_to t('.reorder', :what => "Whats News"),
                   admin_whats_news_url,
                   :id => "reorder_action",
                   :class => "reorder_icon" %>

      <%= link_to t('.reorder_done', :what => "Whats News"),
                   admin_whats_news_url,
                   :id => "reorder_action_done",
                   :style => "display: none;",
                   :class => "reorder_icon" %>
    </li>
  <% end %>
  </ul>
</div>
<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
    <% if @whats_news.any? %>
      <%= will_paginate @whats_news, :previous_label => '&laquo;', :next_label => '&raquo;' %>
      <ul>
        <%= render :partial => "whats_new",
                    :collection => @whats_news %>
      </ul>
      <%= will_paginate @whats_news, :previous_label => '&laquo;', :next_label => '&raquo;' %>
    <% else %>
      <p><%= t('shared.admin.search.no_results') %></p>
    <% end %>
  <% else %>
    <% if @whats_news.any? %>
      <%= will_paginate @whats_news,
                         :previous_label => '&laquo;',
                         :next_label => '&raquo;' %>

      <%= render :partial => "sortable_list" %>

      <%= will_paginate @whats_news,
                         :previous_label => '&laquo;',
                         :next_label => '&raquo;' %>
    <% else %>
      <p>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      </p>
    <% end %>
  <% end %>
</div>
<%= render :partial => "/shared/admin/make_sortable",
            :locals => {
              :tree => false
            } if !searching? and WhatsNew.count > 1 %>
